from cryptography.fernet import Fernet

# Step 1: Generate a key
key = Fernet.generate_key()
print("DECRYPTION_SECRET =", key.decode())

# Step 2: Encrypt your actual API key
fernet = Fernet(key)
encrypted = fernet.encrypt(b"your-real-api-key")
print("ENCRYPTED_API_KEY =", encrypted.decode())
